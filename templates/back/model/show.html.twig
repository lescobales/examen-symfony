{% extends 'front/base.html.twig' %}

{% block title %}{{ 'form.listing.title'|trans }}{% endblock %}

{% block body %}
    <h1>Model</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ model.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_model_index') }}">back to list</a>

    <a href="{{ path('app_model_edit', {'id': model.id}) }}">edit</a>

    {{ include('back/model/_delete_form.html.twig') }}
{% endblock %}
